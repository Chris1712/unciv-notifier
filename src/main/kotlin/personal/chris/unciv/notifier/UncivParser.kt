package personal.chris.unciv.notifier

import java.nio.file.Path

class UncivParser {

    companion object {
        fun getNextTurn(saveFile: Path): String {
            // TODO
            // load file
            // base64 -> gzip -> not-actually-json
            // figure out whose turn it is
            // return that (uuid I guess?)
            return "Chris"
        }
    }
}